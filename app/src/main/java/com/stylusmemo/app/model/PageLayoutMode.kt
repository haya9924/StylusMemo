package com.stylusmemo.app.model

/** How pages are arranged in the editor view. */
enum class PageLayoutMode(val displayName: String) {
    SINGLE("単一ページ"),
    VERTICAL("縦スクロール"),
    HORIZONTAL("横スクロール"),
}
